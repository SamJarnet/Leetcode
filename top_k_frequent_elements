def topKFrequent(nums: list[int], k: int) -> list[int]:
    hash = {nums[0]: 1}
    for i in range(1, len(nums)):
        if nums[i] in hash:
            hash[nums[i]] += 1
        else:
            hash.update({nums[i]: 1})
    return(list(dict(sorted(hash.items(), key=lambda item: item[1])).keys())[-k:])



print(topKFrequent([1,2,2,3,3,3], 2))